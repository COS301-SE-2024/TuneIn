import 'package:flutter/material.dart';

class RegisterOtherScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Register Other'),
      ),
      body: Center(
        child: Text(
          'Register Other Screen',
          style: TextStyle(fontSize: 24),
        ),
      ),
    );
  }
}
